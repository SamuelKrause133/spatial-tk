# Pip deps for `./venv_image` (after conda: Python + JDK + numpy/scipy; see makefile).
# Order-sensitive packages are preceded by numpy/scipy in `make venv-image`.

numpy==1.26.4
scipy==1.11.4
setuptools<81

tomli>=1.2.0
pandas==2.3.3
matplotlib==3.10.9
seaborn==0.13.2
pillow>=10,<11
scikit-image>=0.22,<0.25
tifffile>=2023,<2026
pims>=0.7,<0.8
python-bioformats==4.1.0
torch>=2.1,<3
torchvision>=0.16,<1
cellpose==2.0.5
pytest>=7.0
