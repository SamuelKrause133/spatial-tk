# Legacy convenience entrypoint (deprecated).
#
# This project maintains two pinned dependency stacks:
# - analysis: requirements-analysis.txt / `pip install -e ".[analysis]"`
# - image:    requirements-image.txt   / `pip install -e ".[image]"`
#
# `requirements.txt` is kept only to avoid breaking existing workflows that do:
#   pip install -r requirements.txt
#
# It now aliases the pinned analysis stack.
-r requirements-analysis.txt
